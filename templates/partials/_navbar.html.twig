<header>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">LB</a>
                <form class="d-flex" role="search">
                    <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
                    <button class="btn btn-outline-success" type="submit">Search</button>
                </form>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNavDropdown">
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page" href="{{ path('app_users_show') }}">Mon Profil</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="#">Mes Reservations</a>
                        </li>
                        <li class="nav-item">
                          <a class="nav-link" href="#">Mon Abonnement</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="#"></a>
                        </li>
                    </ul>
                </div>
                <div> 
                    <a href="{{ path('change_locale', {'locale': 'en'}) }}" class="btn btn-primary">English</a> 
                    <a href="{{ path('change_locale', {'locale': 'fr'}) }}" class="btn btn-primary">Français</a>
                </div>    
            </div>
        </nav>
</header>

{#
<ul>
    <li><a href="{{ path('home') }}">Home</a></li>
    <li><a href="{{ path('catalogue') }}">Catalogue</a></li>
    <li><a href="{{ path('reservations') }}">Reservations</a></li>
    {% if app.user %}
        <li><a href="{{ path('dashboard') }}">Dashboard</a></li>
        <li><a href="{{ path('logout') }}">Logout</a></li>
    {% else %}
        <li><a href="{{ path('login') }}">Login</a></li>
        <li><a href="{{ path('register') }}">Register</a></li>
    {% endif %}
</ul>
#}