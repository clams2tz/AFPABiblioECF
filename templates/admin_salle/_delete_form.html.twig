<form method="post" action="{{ path('app_admin_salle_delete', {'id': salle_de_travail.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ salle_de_travail.id) }}">
    <button class="btn">Delete</button>
</form>
